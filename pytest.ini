[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: real SDXL pipeline load or real subprocess spawn -- excluded from the default run (see tests/slow/), opt in with -m slow
addopts = -m "not slow"
